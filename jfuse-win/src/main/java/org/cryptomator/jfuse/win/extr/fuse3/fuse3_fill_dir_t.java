// Generated by jextract

package org.cryptomator.jfuse.win.extr.fuse3;

import java.lang.invoke.*;
import java.lang.foreign.*;
import java.nio.ByteOrder;
import java.util.*;
import java.util.function.*;
import java.util.stream.*;

import static java.lang.foreign.ValueLayout.*;
import static java.lang.foreign.MemoryLayout.PathElement.*;

/**
 * {@snippet lang=c :
 * typedef int (*fuse3_fill_dir_t)(void *, const char *, const struct fuse_stat {
 *     fuse_dev_t st_dev;
 *     fuse_ino_t st_ino;
 *     fuse_mode_t st_mode;
 *     fuse_nlink_t st_nlink;
 *     fuse_uid_t st_uid;
 *     fuse_gid_t st_gid;
 *     fuse_dev_t st_rdev;
 *     fuse_off_t st_size;
 *     struct fuse_timespec st_atim;
 *     struct fuse_timespec st_mtim;
 *     struct fuse_timespec st_ctim;
 *     fuse_blksize_t st_blksize;
 *     fuse_blkcnt_t st_blocks;
 *     struct fuse_timespec st_birthtim;
 * } *, fuse_off_t, enum fuse3_fill_dir_flags {
 *     FUSE_FILL_DIR_PLUS = (1 << 1)
 * })
 * }
 */
public class fuse3_fill_dir_t {

    fuse3_fill_dir_t() {
        // Should not be called directly
    }

    /**
     * The function pointer signature, expressed as a functional interface
     */
    public interface Function {
        int apply(MemorySegment buf, MemorySegment name, MemorySegment stbuf, long off, int flags);
    }

    private static final FunctionDescriptor $DESC = FunctionDescriptor.of(
        fuse_h.C_INT,
        fuse_h.C_POINTER,
        fuse_h.C_POINTER,
        fuse_h.C_POINTER,
        fuse_h.C_LONG_LONG,
        fuse_h.C_INT
    );

    /**
     * The descriptor of this function pointer
     */
    public static FunctionDescriptor descriptor() {
        return $DESC;
    }

    private static final MethodHandle UP$MH = fuse_h.upcallHandle(fuse3_fill_dir_t.Function.class, "apply", $DESC);

    /**
     * Allocates a new upcall stub, whose implementation is defined by {@code fi}.
     * The lifetime of the returned segment is managed by {@code arena}
     */
    public static MemorySegment allocate(fuse3_fill_dir_t.Function fi, Arena arena) {
        return Linker.nativeLinker().upcallStub(UP$MH.bindTo(fi), $DESC, arena);
    }

    private static final MethodHandle DOWN$MH = Linker.nativeLinker().downcallHandle($DESC);

    /**
     * Invoke the upcall stub {@code funcPtr}, with given parameters
     */
    public static int invoke(MemorySegment funcPtr,MemorySegment buf, MemorySegment name, MemorySegment stbuf, long off, int flags) {
        try {
            return (int) DOWN$MH.invokeExact(funcPtr, buf, name, stbuf, off, flags);
        } catch (Throwable ex$) {
            throw new AssertionError("should not reach here", ex$);
        }
    }
}

